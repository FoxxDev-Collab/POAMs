@model POAMs.Web.Models.Domain.NessusTestCase
@{
    ViewData["Title"] = "Add Vulnerability Finding";
    var stp = ViewBag.STP as POAMs.Web.Models.Domain.SecurityTestPlan;
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index">Test Plans</a></li>
        <li class="breadcrumb-item"><a asp-action="Details" asp-route-id="@stp?.Id">@stp?.STPIdentifier</a></li>
        <li class="breadcrumb-item active">Add Vulnerability</li>
    </ol>
</nav>

<h1 class="h3 mb-4">
    <i class="bi bi-bug me-2"></i>Add Vulnerability Finding
</h1>

<form asp-action="AddNessusTestCase" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="SecurityTestPlanId" />

    <div class="row">
        <div class="col-lg-6">
            <div class="card mb-4">
                <div class="card-header">
                    <i class="bi bi-info-circle me-2"></i>Plugin Information
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="PluginId" class="form-label">Plugin ID</label>
                            <input asp-for="PluginId" class="form-control" placeholder="e.g., 12345" />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="PluginFamily" class="form-label">Plugin Family</label>
                            <input asp-for="PluginFamily" class="form-control" placeholder="e.g., Windows" />
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="PluginName" class="form-label">Plugin Name</label>
                        <input asp-for="PluginName" class="form-control" placeholder="e.g., SSL Certificate Expired" />
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="CVE" class="form-label">CVE</label>
                            <input asp-for="CVE" class="form-control" placeholder="e.g., CVE-2024-1234" />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="Severity" class="form-label">Severity</label>
                            <select asp-for="Severity" class="form-select">
                                <option value="Critical">Critical</option>
                                <option value="High">High</option>
                                <option value="Medium" selected>Medium</option>
                                <option value="Low">Low</option>
                                <option value="Info">Informational</option>
                            </select>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="CVSSScore" class="form-label">CVSS Score</label>
                            <input asp-for="CVSSScore" class="form-control" placeholder="e.g., 9.8" />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="CVSSVector" class="form-label">CVSS Vector</label>
                            <input asp-for="CVSSVector" class="form-control" placeholder="CVSS:3.1/AV:N/AC:L/..." />
                        </div>
                    </div>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <i class="bi bi-pc-display me-2"></i>Affected Assets
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="AffectedHosts" class="form-label">Affected Hosts</label>
                        <textarea asp-for="AffectedHosts" class="form-control" rows="2"
                                  placeholder="Comma-separated list of hostnames/IPs"></textarea>
                    </div>
                    <div class="mb-3">
                        <label asp-for="AffectedPorts" class="form-label">Affected Ports</label>
                        <input asp-for="AffectedPorts" class="form-control" placeholder="e.g., 443/tcp, 8443/tcp" />
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-6">
            <div class="card mb-4">
                <div class="card-header">
                    <i class="bi bi-file-text me-2"></i>Finding Details
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="Synopsis" class="form-label">Synopsis</label>
                        <textarea asp-for="Synopsis" class="form-control" rows="2"></textarea>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Description</label>
                        <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Solution" class="form-label">Solution</label>
                        <textarea asp-for="Solution" class="form-control" rows="3"></textarea>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PluginOutput" class="form-label">Plugin Output</label>
                        <textarea asp-for="PluginOutput" class="form-control font-monospace" rows="3"
                                  placeholder="Original scan output"></textarea>
                    </div>
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header">
                    <i class="bi bi-clipboard-check me-2"></i>Test Results
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="Status" class="form-label">Status</label>
                        <select asp-for="Status" class="form-select">
                            <option value="NotTested">Not Tested</option>
                            <option value="InProgress">In Progress</option>
                            <option value="Remediated">Remediated</option>
                            <option value="PartiallyRemediated">Partially Remediated</option>
                            <option value="NotRemediated">Not Remediated</option>
                            <option value="FalsePositive">False Positive</option>
                            <option value="AcceptedRisk">Accepted Risk</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="RemediationSteps" class="form-label">Remediation Steps</label>
                        <textarea asp-for="RemediationSteps" class="form-control" rows="2"
                                  placeholder="What was done to fix the issue"></textarea>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Evidence" class="form-label">Evidence</label>
                        <textarea asp-for="Evidence" class="form-control" rows="2"
                                  placeholder="Proof of remediation"></textarea>
                    </div>

                    <div class="form-check mb-3">
                        <input asp-for="RescanRequired" class="form-check-input" type="checkbox" checked />
                        <label asp-for="RescanRequired" class="form-check-label">Rescan Required</label>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-between">
        <a asp-action="Details" asp-route-id="@Model.SecurityTestPlanId" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left me-1"></i>Cancel
        </a>
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-check-circle me-1"></i>Add Finding
        </button>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
